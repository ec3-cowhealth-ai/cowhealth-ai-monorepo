# Archived Frontend Implementation Plan

This plan is a historical snapshot of how the frontend was built.

- It should not be used as the source of truth for the current repo state.
- Use [docs/README.md](/docs/README.md) for the current documentation map.
- Use the feature READMEs under `frontend/src/features/` for current feature-level notes.

